<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/data/mongo
        http://www.springframework.org/schema/data/mongo/spring-mongo.xsd">

    <!-- Needs for use MVC annotations
    <mvc:annotation-driven/>  -->

    <!-- Needs for @Service, @Repository and etc.  -->
    <context:annotation-config/>

    <!-- Sets location for static css files
    <mvc:resources mapping="/css/**" location="/css/"/>

    <context:component-scan base-package="com.epam.lab.news"/>    -->

    <!-- Needs for identify views
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/pages/"/>
        <property name="suffix" value=".jsp"/>
    </bean>                       -->

    <!--
    <mongo:mongo host="localhost" port="27017">
        <mongo:options connections-per-host="16"
                       threads-allowed-to-block-for-connection-multiplier="4"
                       connect-timeout="100"
                       max-wait-time="500"
                       auto-connect-retry="true"
                       socket-keep-alive="true"
                       socket-timeout="500"
                       slave-ok="true"
                       write-number="1"
                       write-timeout="0"
                       write-fsync="true" />
    </mongo:mongo>

    <mongo:db-factory id="mongoDbFactory"
                      host="localhost"
                      port="27017"
                      dbname="news" />

    <bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
        <constructor-arg name="mongoDbFactory" ref="mongoDbFactory"/>
    </bean>

    <mongo:mongo id="mongoConnection" host="localhost" port="27017" write-concern="1" />      -->

    <!-- Need for working with NewsRepository by autowiring
    <mongo:repositories base-package="com.epam.lab.news.data.repo"/>   -->

    <!--
    <bean id="sessionLocaleResolver"
          class="org.springframework.web.servlet.i18n.SessionLocaleResolver">
        <property name="defaultLocale" value="en"/>
    </bean>
    <mvc:interceptors>
        <bean id="localeChangeInterceptor"
              class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor">
            <property name="paramName" value="lang"/>
        </bean>
    </mvc:interceptors>

    <bean id="resourceBundleMessageSource"
          class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basename" value="interface"/>
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

    <bean id="controllerClassNameHandlerMapping"
          class="org.springframework.web.servlet.mvc.support.ControllerClassNameHandlerMapping">
        <property name="interceptors">
            <list>
                <ref bean="localeChangeInterceptor"/>
            </list>
        </property>
    </bean>
    -->

</beans>